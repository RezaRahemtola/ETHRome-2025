# XMTP Agent Configuration
# All values WITHOUT quotes!

# Your wallet's private key (with 0x prefix)
XMTP_WALLET_KEY=0x...

# XMTP network: "dev" or "production"
XMTP_ENV=dev

# Database encryption key (32-byte hex with 0x prefix)
# Generate with: node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
XMTP_DB_ENCRYPTION_KEY=0x...

# Database path for persistent storage (IMPORTANT for production!)
# Leave blank or comment out for in-memory (data lost on restart)
XMTP_DB_PATH=./agent-db

# Group mappings database path (SQLite)
# Stores event address -> XMTP group ID mappings
GROUP_DB_PATH=./group-mappings.db

# Factory contract address on Base mainnet
FACTORY_CONTRACT_ADDRESS=0xFb7B5cadb3EB1dA033Ba1872fa42eB2900b26491

# Base RPC URL (optional - defaults to https://mainnet.base.org)
BASE_RPC_URL=https://api.developer.coinbase.com/rpc/v1/base/YOUR_API_KEY

# Debug: Allow creating groups with less than 2 members (will be DMs)
# Set to "true" for testing, leave blank for production
# ALLOW_SMALL_GROUPS=true
